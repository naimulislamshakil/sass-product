import axios from 'axios';
// import { userData } from '../Slices/authSlice';

export const baseUrl = 'http://localhost:5000';

// export const registerUserApi = async (data: userData | void) => {
// 	const res = await axios.post(`${baseUrl}/singup`, data);
// 	return res.data;
// };
